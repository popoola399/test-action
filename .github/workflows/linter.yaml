name: OpenSearch Linter 
on: push

jobs:
	job_1:
		runs-on: ubuntu-latest
		steps: 
			- uses: action/checkout@v2
			- uses: action/setup-node@v1
			  with:
				node-version: 12
			- run: npm ci
			- run: npm index.js
